<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>SD-WAN on Barash Helvadzhaoglu</title><link>https://barashhelvadzhaoglu.com/en/tags/sd-wan/</link><description>Recent content in SD-WAN on Barash Helvadzhaoglu</description><generator>Hugo -- 0.163.2</generator><language>en</language><lastBuildDate>Fri, 12 Jun 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://barashhelvadzhaoglu.com/en/tags/sd-wan/index.xml" rel="self" type="application/rss+xml"/><item><title>What Is SD-WAN? A Practical Guide for Network Engineers</title><link>https://barashhelvadzhaoglu.com/en/technology/what-is-sdwan/</link><pubDate>Fri, 12 Jun 2026 00:00:00 +0000</pubDate><guid>https://barashhelvadzhaoglu.com/en/technology/what-is-sdwan/</guid><description>A comprehensive technical guide to SD-WAN: how it works, key components, traffic steering, and why enterprises are replacing MPLS with software-defined WAN.</description><content:encoded><![CDATA[<h1 id="what-is-sd-wan-a-practical-guide-for-network-engineers">What Is SD-WAN? A Practical Guide for Network Engineers</h1>
<p>Enterprise WAN has a problem. For decades, organizations have paid premium prices for MPLS circuits to guarantee application performance between branch offices, data centers, and headquarters. The architecture made sense when all traffic was internal and all applications lived in the data center. Then cloud happened.</p>
<p>Today, a branch office in Munich sends its Microsoft 365 traffic through a 200 ms MPLS backhaul to Frankfurt, then out to the internet, then to Microsoft&rsquo;s European edge — adding 60 to 100 ms of unnecessary latency to every Teams call and SharePoint sync. The network was built for a world that no longer exists.</p>
<p>SD-WAN — Software-Defined Wide Area Network — is the architectural answer to this problem. This article explains what it is, how it works, and why it has become the default WAN architecture for enterprise networks.</p>
<hr>
<h2 id="the-core-idea">The Core Idea</h2>
<p>SD-WAN separates the <strong>control plane</strong> from the <strong>data plane</strong> in WAN connectivity. In a traditional router-based WAN, each device makes its own forwarding decisions based on static routing tables or simple metrics. An SD-WAN overlay decouples that intelligence into a centralized controller that has visibility across all sites, all links, and all applications simultaneously.</p>
<p>The result is a WAN that can make intelligent, real-time decisions: send this video conference over the 4G LTE link because the MPLS link has 2% packet loss right now; route this SAP transaction over MPLS because it requires low jitter; send this software update over cheap broadband because it can tolerate delay.</p>
<p>This is <strong>application-aware routing</strong> — the defining capability of SD-WAN.</p>
<hr>
<h2 id="key-components">Key Components</h2>
<h3 id="edge-devices-sd-wan-appliances">Edge Devices (SD-WAN Appliances)</h3>
<p>Physical or virtual devices deployed at each site. They terminate WAN links, build encrypted overlay tunnels to other sites, and enforce locally the policies distributed by the controller. Most vendors offer hardware appliances, virtual appliances (for cloud deployments), and software agents.</p>
<h3 id="sd-wan-controller--orchestrator">SD-WAN Controller / Orchestrator</h3>
<p>The centralized brain. It maintains a real-time view of all edges, all links, and their current performance metrics. Network administrators define policies at the controller level — which applications use which links, what quality thresholds trigger failover, how security policies are applied. The controller pushes these policies to all edges automatically.</p>
<h3 id="centralized-management-portal">Centralized Management Portal</h3>
<p>A single dashboard for monitoring, troubleshooting, and configuration across all sites. This is one of the primary operational advantages of SD-WAN: a network engineer can see the WAN state of 200 branch offices on a single screen and push a configuration change to all of them in minutes.</p>
<h3 id="underlay-transport">Underlay Transport</h3>
<p>SD-WAN is transport-agnostic. The overlay tunnels can run over any combination of:</p>
<ul>
<li>MPLS</li>
<li>Broadband internet (fiber, cable)</li>
<li>4G/5G LTE</li>
<li>Satellite (increasingly relevant with LEO options)</li>
</ul>
<p>Most deployments use a <strong>hybrid underlay</strong> — retaining MPLS for latency-sensitive applications while adding cheaper broadband for general internet traffic.</p>
<hr>
<h2 id="how-traffic-steering-works">How Traffic Steering Works</h2>
<p>The intelligence of SD-WAN lives in its traffic steering engine. Here is how a typical deployment makes forwarding decisions:</p>
<p><strong>1. Application Identification</strong>
The edge device classifies traffic using deep packet inspection (DPI), identifying not just the destination IP but the actual application. It knows the difference between a Teams video call and a Teams file download, between SAP GUI traffic and background SAP synchronization.</p>
<p><strong>2. Link Quality Monitoring</strong>
Every SD-WAN edge continuously measures the performance of each WAN link using active probes: latency, jitter, packet loss, and available bandwidth. These measurements happen every few seconds, giving the system near real-time visibility.</p>
<p><strong>3. Policy Matching</strong>
When a packet arrives, the edge matches it against the policy table: &ldquo;Microsoft Teams video — requires &lt; 150 ms latency, &lt; 1% packet loss, &lt; 30 ms jitter. Preferred path: MPLS. Fallback: broadband. Last resort: LTE.&rdquo;</p>
<p><strong>4. Dynamic Path Selection</strong>
The edge selects the path that currently meets the policy requirements. If the MPLS link degrades beyond the threshold, failover to broadband happens in milliseconds — typically faster than the application&rsquo;s own timeout mechanism, meaning users often don&rsquo;t notice.</p>
<p><strong>5. Packet-Level Techniques</strong>
Advanced SD-WAN implementations use packet-level techniques to improve performance further:</p>
<ul>
<li><strong>Forward Error Correction (FEC):</strong> sends redundant packets so the receiver can reconstruct lost data without retransmission</li>
<li><strong>Packet duplication:</strong> sends critical packets over multiple paths simultaneously, delivering whichever arrives first</li>
<li><strong>WAN optimization:</strong> deduplication, compression, and TCP optimization to reduce bandwidth consumption</li>
</ul>
<hr>
<h2 id="sd-wan-vs-mpls-what-actually-changes">SD-WAN vs. MPLS: What Actually Changes</h2>
<p>MPLS is not going away entirely — it still provides deterministic latency guarantees that internet broadband cannot match for critical applications. What changes is the role it plays.</p>
<p>In a traditional WAN, MPLS carried everything. In an SD-WAN deployment, MPLS carries only the applications that genuinely require its guarantees: latency-sensitive voice and video, real-time ERP transactions, manufacturing control systems. Everything else — cloud application traffic, internet browsing, software updates, backups — moves to cheaper broadband links.</p>
<p>The practical outcome for most enterprises: WAN costs decrease significantly while application performance for cloud workloads improves, because traffic no longer makes unnecessary backhauls through data center gateways.</p>
<hr>
<h2 id="direct-internet-access-and-cloud-on-ramps">Direct Internet Access and Cloud On-Ramps</h2>
<p>One of the most valuable SD-WAN capabilities for modern enterprises is <strong>Direct Internet Access (DIA)</strong> at the branch. Instead of routing all internet traffic through a central hub, branch offices connect directly to the internet.</p>
<p>This requires that security be enforced locally or in the cloud — which is where SD-WAN increasingly integrates with cloud-delivered security (firewall-as-a-service, secure web gateway, CASB). The combination of SD-WAN with cloud security is the foundation of the SASE architecture, though that is a separate topic.</p>
<p>Major cloud providers have also built <strong>SD-WAN cloud on-ramp</strong> capabilities: optimized entry points into AWS, Azure, and Google Cloud that SD-WAN controllers can automatically route cloud-destined traffic toward. This eliminates the performance penalty of backhauling cloud traffic through a data center before it exits to the internet.</p>
<hr>
<h2 id="deployment-models">Deployment Models</h2>
<p><strong>Managed SD-WAN</strong>
The SD-WAN infrastructure is deployed and operated by a service provider. The enterprise receives a managed service with SLAs. Common for organizations without the internal network engineering capacity to operate SD-WAN independently.</p>
<p><strong>DIY / Enterprise-Managed SD-WAN</strong>
The enterprise purchases SD-WAN appliances and licenses directly from the vendor and operates the infrastructure internally. Provides maximum control and flexibility. Requires skilled network engineering staff.</p>
<p><strong>Cloud-Hosted Controller</strong>
Most vendors now offer their controller as a SaaS service, eliminating the need to operate controller infrastructure internally. The edges are on-premises; the control plane is cloud-hosted.</p>
<hr>
<h2 id="where-sd-wan-makes-the-most-sense">Where SD-WAN Makes the Most Sense</h2>
<p>SD-WAN delivers clear value in specific scenarios:</p>
<ul>
<li><strong>Distributed retail or hospitality:</strong> dozens to hundreds of sites, each requiring reliable connectivity for POS systems, digital signage, and guest WiFi — with minimal on-site IT staff</li>
<li><strong>Manufacturing with multiple plants:</strong> real-time OT/IT convergence requirements alongside standard enterprise traffic</li>
<li><strong>Financial services branches:</strong> strict application performance requirements for core banking applications, with cost pressure to reduce MPLS footprint</li>
<li><strong>Healthcare networks:</strong> clinic-to-clinic connectivity with GDPR/HIPAA compliance requirements and high sensitivity to network outages</li>
</ul>
<hr>
<h2 id="what-sd-wan-does-not-solve">What SD-WAN Does Not Solve</h2>
<p>It is worth being direct about the limitations:</p>
<p>SD-WAN does not eliminate the need for skilled network engineering. The policies that drive application-aware routing must be correctly defined. A misconfigured SD-WAN can route latency-sensitive traffic over unreliable links just as easily as a correctly configured one routes it optimally.</p>
<p>SD-WAN also does not inherently provide security. An SD-WAN overlay is an encrypted tunnel between sites, but it does not inspect traffic for threats. Security must be layered on top — either through integrated security functions in the SD-WAN appliance (as Fortinet does with FortiOS) or through integration with cloud-delivered security services.</p>
<p>Finally, SD-WAN requires a functioning underlay. If the internet circuits at a branch are unreliable, SD-WAN can fail over between them, but it cannot compensate for a fundamentally poor connectivity environment.</p>
<hr>
<h2 id="summary">Summary</h2>
<p>SD-WAN is a mature, proven architecture for enterprise WAN. It delivers measurable improvements in application performance for cloud workloads, reduces WAN costs by enabling hybrid underlay with cheaper broadband, and dramatically simplifies WAN operations through centralized management.</p>
<p>The major vendors — Fortinet, Cisco, HPE Aruba, and Palo Alto — each take a meaningfully different approach to the architecture. Understanding those differences matters when selecting a platform, which is what the following articles in this series examine in detail.</p>
<hr>
<p><em>This article is part of a series on SD-WAN architectures. Next: <a href="/en/posts/fortinet-sd-wan/">Fortinet SD-WAN — Security-Driven Networking in Practice</a></em></p>
]]></content:encoded></item></channel></rss>